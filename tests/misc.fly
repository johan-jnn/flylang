|kind Dividable(
  #(private, readonly)
  fn get_num -> num()

  fn multiple_of(num n, me.num() % n = 0)
)

fn
<T: Dividable>
mystere -> int({T} arr: {},
  each(arr, item, index,
    if(item.multiple_of(3), return @mystere index)
  )
  return -1
)|

fn my_func @main(a, b,
  fn test(val,
    return @val if(val, val, false)
  );
  return test(a, b);
)
